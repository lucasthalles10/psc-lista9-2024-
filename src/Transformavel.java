public interface Transformavel {
    // Declaração de um método abstrato transformar que aceita um nível como parâmetro
    // e retorna uma string indicando o resultado da transformação
    String transformar(int nivel);
}